import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Workshops & Audits",
  description: "Book a tailored brand or website audit, or a strategy workshop with Nectarine Studio — clear, actionable output every session.",
};

export default function WorkshopsAuditsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
